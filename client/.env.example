# Base URL of the backend API. Vite's dev proxy (vite.config.js) already
# forwards /api to localhost:5000, so this is mainly needed for production builds.
VITE_API_BASE_URL=http://localhost:5000/api
